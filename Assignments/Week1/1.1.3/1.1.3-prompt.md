# Prompt Used 
@workspace Trace the lifecycle of a health check request for the Cart Service.

Entry Point: Find the route definition (e.g., /health or /liveness).

Logic Chain: Identify the 'Checker' or 'Service' class that performs the actual validation.

Dependencies: List every external resource this check pings. Does it check the Cart DB? A cache layer?

Trigger: Confirm if this is an active HTTP endpoint or a background heartbeat.

Summarize the flow and pinpoint the file paths.


